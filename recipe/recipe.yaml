schema_version: 1

context:
  version: "0.1.2"

package:
  name: sphinx-fuma
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/sphinx-fuma/sphinx_fuma-${{ version }}.tar.gz
  sha256: d0691d021dc3da7e87ded6f10f98c16ce13cbaee98c7b05800f5b1557accf6bc

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - hatchling
    - pip
    - python ${{ python_min }}.*
  run:
    - python >=${{ python_min }}
    - sphinx >=7.2.6,<9
    - sphinx-searchlite

tests:
  - python:
      imports:
        - sphinx_fuma
      python_version:
        - ${{ python_min }}.*
        - "*"
      pip_check: true

about:
  homepage: https://github.com/python-project-templates/sphinx-fuma
  summary: Fumadocs theme for Sphinx
  license: Apache-2.0 AND OFL-1.1
  license_file:
    - LICENSE
    - sphinx_fuma/static/fonts/Geist-LICENSE.txt
    - sphinx_fuma/static/fonts/JetBrainsMono-LICENSE.txt
  repository: https://github.com/python-project-templates/sphinx-fuma

extra:
  recipe-maintainers:
    - timkpaine
